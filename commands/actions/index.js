const sL = require('./lang');
const cN = require('./new_comp')
const init = require('./init')
const info = require('./info')

module.exports = {
    selectLanguage : sL,
    newComp : cN,
    init,
    info,
}